# CargoGo v1.4.4 — Professional UI typecheck hotfix

- Guarded nullable `readJsonSafe<P>()` responses in carrier profile load/save.
- Replaced invalid `BrandIcon` name `deal` with existing `deals`.
- Updated professional mode icon union to `route | deals`.
- No business logic, API, database or UI layout changes.
