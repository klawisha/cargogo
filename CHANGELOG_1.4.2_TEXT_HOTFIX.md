# CargoGo 1.4.2 — Text node hotfix

- Fixed repeated React Native warning `Text strings must be rendered within a <Text> component`.
- Root cause: an explicit JSX space between two conditional children inside each Badger Run track node.
- No UI, API, schema, or dependency changes.
