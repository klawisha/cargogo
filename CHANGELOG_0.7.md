# v0.7.0
- editable cargo/trips until the first Deal is accepted
- hard delete only before any Deal exists
- pending offers are superseded when terms change
- matches are recomputed after edits
- structured country/city/street model; no lat/lng in mobile UX
- Ukraine alpha city catalog + API picker
- matching v2 score breakdown and city-pair classification
- legacy coordinate records best-effort backfilled to nearest catalog city
- new editability smoke test
